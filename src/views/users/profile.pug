extends ../base 
include ../mixins/video

block content
    div.profile__data 
        img(src="/" + user.avatarUrl).avartar
        h3=user.name 
    div.video-grid 
        each video in user.videos 
            +video(video)
        else 
            span.empty__message No videos found